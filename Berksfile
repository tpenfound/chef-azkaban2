chef_api :config
site :opscode

metadata

#cookbook 'ntp ', '= 1.1.6', chef_api: :config
#^needs correct version number? already included in base role...
#cookbook "java", "= 0.2.0", chef_api: :config 
#^needs correct version number? typically would need logic in role-java..

